# Обновление characters.txt только именами умерших

# Открываем файл с мертвыми персонажами
with open('dead-characters.txt', 'r', encoding='utf-8') as file:
    dead_characters = [line.strip() for line in file]

# Записываем эти имена в файл characters.txt
with open('characters.txt', 'w', encoding='utf-8') as file:
    for character in dead_characters:
        file.write(f"{character}\n")

print("characters.txt обновлен только с мертвыми персонажами.")
